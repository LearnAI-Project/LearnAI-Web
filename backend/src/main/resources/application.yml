spring:
  application:
    name: LearnAi
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/lai_db
    username: postgres
    password: postgres

app:
  cors:
    allowed-origins: http://localhost:5173/

logging:
  level:
    org.springframework.security: DEBUG
    # org.hibernate.SQL: DEBUG